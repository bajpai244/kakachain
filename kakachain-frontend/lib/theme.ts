import {sketchy} from "@theme-ui/presets"
import { Theme } from "theme-ui";

const theme: Theme = {
    ...sketchy,
    colors: {
        ...sketchy.colors,
        background: "wheat",
    }
};


export default theme;